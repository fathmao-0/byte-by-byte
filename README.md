# byte-by-byte
practising probems one at a time.


# My Python Project

 What this project does

A simple Python project that uses an API key securely using environment variables.

## ⚙️ Setup Instructions

1. Clone the repository

2. Create a `.env` file in the project folder

3. Add your API key:
   API_KEY=your_api_key_here

4. Install dependencies:
   pip install python-dotenv

5. Run the program:
   python main.py

## Important

* Do NOT share your `.env` file
* The `.env` file is ignored for security reasons

##  Files

* main.py → main program
* .env.example → shows required environment variables
* .gitignore → hides sensitive and unnecessary files
